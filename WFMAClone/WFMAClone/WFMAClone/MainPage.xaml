<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cond="schema?conditionalMethod(parameter)"
             xmlns:local="clr-namespace:WFMAClone"
             x:Class="WFMAClone.MainPage">

        <ListView x:Name="listView" ItemSelected="OnListItemSelected" HasUnevenRows ="true" BackgroundColor="White">
            <ListView.ItemTemplate>  
                <DataTemplate>  
                    <ViewCell Height="85"> 
                        <StackLayout Orientation="Vertical" Margin="3" Padding="2">
                            <StackLayout Orientation="Horizontal">
                                    <StackLayout Orientation="Vertical">  
                                        <StackLayout Orientation="Horizontal"> 
                                            <Label Text="{Binding JobId}" Font="18"></Label>    
                                            <Label Text="{Binding Name}" Font="18"></Label>  
                                        </StackLayout>  

                                        <Label Text="{Binding JobType}" TextColor="#3d3d3d"></Label>
                                        <StackLayout Orientation="Horizontal" HorizontalOptions="Start" VerticalOptions="End"> 
                                            <Label Text="Due Date: " TextColor="#3d3d3d"></Label>
                                        <Label Text="{Binding DueDate}" TextColor="#3d3d3d"></Label>
                                        </StackLayout> 
                                    </StackLayout> 
                            
                                    <StackLayout Orientation="Horizontal" HorizontalOptions="EndAndExpand"> 
                                        <StackLayout Orientation="Vertical" HorizontalOptions="EndAndExpand">  
                                            <Label Text="{Binding Status}" HorizontalOptions="EndAndExpand" VerticalOptions="Start"></Label>  

                                            <StackLayout Orientation="Horizontal" HorizontalOptions="EndAndExpand" VerticalOptions="EndAndExpand"> 
                                                <Label Text="Priority: "></Label>
                                              <Label Text="{Binding TaskPriority}"></Label>
                                            </StackLayout> 
                                        
                                        </StackLayout>
                                        <Frame x:Name="statusColor" VerticalOptions="CenterAndExpand" HeightRequest="75" BackgroundColor="Lime" Margin="2" Padding="5" HasShadow="false"></Frame>

                                    </StackLayout>
                                </StackLayout>  
                           </StackLayout>  
                    </ViewCell>  
                </DataTemplate>  
            </ListView.ItemTemplate>  
        </ListView>
</ContentPage>