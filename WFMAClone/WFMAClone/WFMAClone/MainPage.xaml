<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WFMAClone.MainPage">
    <StackLayout>
    <Grid BackgroundColor="Red"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="8*" />
        </Grid.ColumnDefinitions>
        <Label Text="Task List" Margin="5,25,0,8" TextColor="White"></Label>
    </Grid>


    <ListView x:Name="listView" ItemSelected="OnItemSelected" HasUnevenRows ="true"  SeparatorVisibility="None">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Frame BorderColor="#e5e5e5" Margin="3,10,3,0" Padding="10,5,5,5" HasShadow="false" >
                        <StackLayout Orientation="Horizontal">
                            <StackLayout Orientation="Vertical">
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding Name}" TextColor="{Binding Color}"  FontSize="Small"></Label>
                                </StackLayout>
                                <Label Text="{Binding JobType}" FontSize="Small" TextColor="#3d3d3d"></Label>
                                <StackLayout Orientation="Horizontal" HorizontalOptions="Start" VerticalOptions="End">
                                    <Label Text="Due Date" FontSize="Micro"  TextColor="#3d3d3d"></Label>
                                    <Label Text="{Binding ShortDate}" FontSize="Micro" TextColor="#3d3d3d"></Label>
                                </StackLayout>
                            </StackLayout>

                            <StackLayout Orientation="Vertical" HorizontalOptions="EndAndExpand">
                                <Label Text="{Binding Status}" HorizontalOptions="EndAndExpand" VerticalOptions="Start"></Label>

                                <StackLayout Orientation="Horizontal" HorizontalOptions="EndAndExpand" VerticalOptions="EndAndExpand">
                                    <Label Text="Priority: " FontSize="Micro"></Label>
                                    <Label Text="{Binding TaskPriority}" FontSize="Micro"></Label>
                                </StackLayout>


                            </StackLayout>
                            <StackLayout  Orientation="Vertical" VerticalOptions="FillAndExpand">
                                <Frame x:Name="statusColor" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"  BackgroundColor="{Binding Color}" Margin="2" Padding="5" HasShadow="false"></Frame>
                            </StackLayout>
                        </StackLayout>
                    </Frame>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
        
    <Grid BackgroundColor="Red"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>

        <Button x:Name="dateFilterButton" Image="calendar.png" Margin="5" Grid.Column="0" BackgroundColor="Transparent" VerticalOptions="Center" Clicked="OnDateFilterButtonClicked"/>
        <Button x:Name="quickFilterButton" Image="filter.png" Margin="5" Grid.Column="1"  BackgroundColor="Transparent" VerticalOptions="Center" Clicked="OnQuickFilterButtonClicked"/>
        <Button x:Name="removeFilterButton" Image="clear_button.png" Margin="5" Grid.Column="2"  BackgroundColor="Transparent" VerticalOptions="Center" Clicked="OnRemoveFilterButtonClicked"/>
    </Grid>

    <!-- <StackLayout Orientation="Horizontal" VerticalOptions="EndAndExpand" BackgroundColor="Red" Padding="10,0,0,0" HorizontalOptions="FillAndExpand">
        <Button x:Name="dateFilterButton" Image="calendar.png" Margin="5" BackgroundColor="Transparent" VerticalOptions="Center" Clicked="OnDateFilterButtonClicked"/>
        <Button x:Name="quickFilterButton" Image="filter.png" Margin="5" BackgroundColor="Transparent" VerticalOptions="Center" Clicked="OnQuickFilterButtonClicked"/>
        <Button x:Name="removeFilterButton" Image="clear_button.png" Margin="5" BackgroundColor="Transparent" VerticalOptions="Center" Clicked="OnRemoveFilterButtonClicked"/>
    </StackLayout> -->
        
 </StackLayout>
</ContentPage>

